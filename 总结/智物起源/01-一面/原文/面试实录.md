# 面试实录 · 智物起源 一面

> 岗位：Java / Agent 后端开发（推断）
> 日期：2026-09-16
> 原始转写：[2026-09-16-智物起源-transcript.txt](2026-09-16-智物起源-transcript.txt)

## ⚠️ 还原说明

- **本转写为单轨混录**，且**面试官的问句几乎全部缺失**——整场转写 90% 以上是「我」方连续输出，面试官只在开场设备调试、工作时长、待遇、反问环节留下可辨识语句。
- 实录中「**面试官问**」凡能听到原文的**照引**；问句缺失的用 `【问句未录清·据答推断】` 标注主题，**不保证逐字**。
- 我的答句按语义断句、加标点，**词序与用词尽量不改变**；确定无疑的语音错字在原位用 `[还原]` 标出（列表见下）；答错、口误、卡壳、**没说完就收住的地方原样保留**。
- 本场转写的**语音识别错字密度显著高于前几场**（同一术语出现多种误识：如"信号量"→ 型号量 / 消耗量 / 先后量），还原表按「首次出现」归类。

### 主要错字/语音识别还原表

| 转写原文（音） | 还原 |
|---|---|
| 问题的重启拆封 / 重启裁缝 | 问题的**重写拆分** |
| 绘画记忆管理 | **会话**记忆管理（全库统一） |
| NCP / CP（服务） | **MCP** |
| 大模型的一个击剑的一个模块 | 大模型的**接入**模块 |
| 这个城里面去进行构件 | 这个**层**里面进行**构建** |
| 要用量 / 电量 | **调用量** |
| 口语的照应 | 口语的**表达**（疑似） |
| NB / 项链解锁 / 二五 | **embedding** / **向量检索** / **BM25** |
| 术语的规划 | 术语的**归一（化）**（疑似） |
| 短期去截图这些常识 | **错误地截取这些术语**（疑似，原意不明） |
| 通过这个相比，还有透 | 通过 **prompt**、还有 **temperature**（疑似） |
| Jason 格式 | **JSON** 格式 |
| 规则的拆分 Doody | 规则的拆分**兜底** |
| 按物理分 | 按**标点**分（疑似） |
| 指问题 | **子**问题 |
| 县城池 / 县城 / 有的县城 | **线程池** / **线程** / 专用的**线程** |
| 支持库 | **知识库** |
| 避免后续的一个 AM 模型的调用 | 避免后续**大模型（LLM）**的调用（疑似） |
| 这个改写的这个泡沫 | 这个改写的这个 **prompt** |
| 调餐 | **调参** |
| 显示的举例 | **显式**的举例 |
| 强制让这些工具报（读/写类型） | 强制让这些工具**上报**（声明）读/写类型 |
| 影响我们主流成一个使用 | 影响我们**主流程**的使用 |
| 接入了真是够本 | 接入了 **（名未录清，疑似框架/SDK 名）** |
| 判断为血操作 | 判断为**写**操作 |
| DVR 就是 Aesop，他一个洋葱中间 | **（DVR 名未录清）**就是 **AOP**，它是一个**洋葱模型** |
| 的 AP 的前面编程 | **AOP 的面向切面编程** |
| 在去掉系的时候 | 在**调这个写**的时候（疑似） |
| 做腺瘤 / 腺瘤 | 做**限流** / **限流** |
| 模型的一个 GP 的资源 | 模型的 **GPU** 资源 |
| 希望在 weeds 这里做一层限流 | 希望在 **Redis** 这里做一层限流 |
| 达到 GQ 那里 | 打到 **GPU** 那里 |
| 一个 YS 一个信号量 | 一个 **Redis** 信号量 |
| 每台实力 / 每一个实力 | 每台**实例** / 每一个**实例** |
| 分配到分配到这个型号量 | 分配到这个**信号量** |
| 让他去做一个浩良哪个站 | 让它去做一个**均衡**（疑似，原意不明） |
| 应类型的一个就是至的一个字段 | **请求类型**的一个**值**的字段（疑似） |
| 在我们外地，是一个原则的时候 | 在我们**外层**、**做原子（操作）**的时候（疑似） |
| 状态及 / 抢账 / 原值抢账 | **状态机** / **抢占** / **原子抢占** |
| 等待抢账 → 授予抢占 | **等待抢占 → 授予抢占** |
| 消耗量已经量 / 先后量 | **信号量** |
| 图管消耗情况 | **Token 消耗情况**（疑似） |
| 评测级 / 评测型 | **评测集** |
| 这个食言 / 首次延迟 | **首字延迟** |
| 大母亲吐出第一个字 | **大模型**吐出第一个字 |
| 意图别的准确率 | **意图识别**的准确率 |
| EMS 这个公共的开源组件 | **（名未录清，疑为 RAGAS）** |
| 上下文上向精确度 / 上向召回率 | **上下文精确度 / 上下文召回率** |
| 产生情节的时候 | 产生**情况**的时候（疑似） |
| 召回的不开 | 召回的 **Top-K**（疑似） |
| LOIS 教具 | **（同上，疑为 RAGAS）工具** |
| 这个技术型的时候 | 这个技术**选型**的时候 |
| Milosh / 密度我是 | **Milvus** |
| PPV / Vet / PPPP 这个 P | **PgVector / PG** |
| 因为我们是融公司 | 因为我们是**金融**公司 |
| 真实的用户的 base | 真实的用户的 **case** |
| 有前端客户端 | 有**前端**（客户端） |
| 其实是 cloud | 其实是 **Claude** |
| Open 扣的 / 扣的话 | **OpenAI** 的 / **GPT** 的话 |
| 靠的是都没更好用一点的 | **Claude 是要更好用一点的**（疑似） |
| 用 A 去画一些原型图 | 用 **AI** 去画原型图 |
| 拿到这个圆形之后 | 拿到这个**原型**之后 |
| 还有用力的一些体检 | 还有**用例**的一些**提效**（疑似） |
| 萨斯软件服务 | **SaaS** 软件服务 |
| 工作手机 | 工作手机（业务名，未误识） |
| GB 赛道 | **To B 赛道**（疑似） |
| 我叫彭世峰 | 我叫 **xxx** — 全库规范：真实人名一律以 xxx 代替 |

---

## 面试官问 / 我答

### Q1 开场 + 面试官介绍公司与业务
**面试官问：** 诶，你好，听到吗？……创业项目做了几个月的时间，主要是做这个工作，用户群是 [To B] 赛道的企业销售业务，企业管理和老板管理员工以及监控员工这个销售的行为过程，包括运营客户、系统平台、硬件结合，与系统 AI 和后台的开发，主要是（涉及）技术体系的业务工作。这边听到我的声音可以比较响亮吗？

**我：**（回应当前声）

### Q2 自我介绍
**面试官问：**（开场让自我介绍，转写内无问句）

**我：** 好的好的，目前呢，就是我叫xxx，那我来自新疆大学软件工程专业，目前我在中国平安人寿保险股份有限公司担任 Java 开发工程师。

那么我们目前做的一些项目呢，其实就是（问答）系统，那么目前其实是在升级到这个过程中。那我深度参与了**问题的重写拆分**这个模块，然后**意图识别**这个模块，然后**会话记忆管理**，然后的**检索召回识别**，还有**分布式的一个限流**。那么目前的主要的工作其实（就）是这些。

然后我们这个项目上（现在）保持的（是）**日均 50,000 的一个调用量**，服务了我们公司内部的用户有 **1100 多个**。

那我们这个项目其实是使用 Java 技术来进行一个搭建的，然后我们接入的我们就是 [Java book（名未录清，疑为技术栈）]，然后数据库使用的数据库，其实就是……那么目前的话，我们这个系统呢，其实是分为了**（第一）层**就是我们的（接口层），那么我们跟相关一些接口一些流程的一些功能，那么都会在这个层里面去进行构建，然后往下的话就是有一个 **MCP 层**，那么 MCP 是我们一个单独服务，然后的话用于调用这些工具，用于就是 agent 或者说模型调用这些相关的工具。中间那一层呢，还有一个就是我们的大模型的一个**接入**的一个模块，（最）底层就是**数据库模块**。那么我们目前是有这四个模块来构成的。那我目前介绍的就是这么多。

### Q3 项目深挖：问题重写拆分——为什么要做？三层结构怎么做？

**面试官问：**（问句未录清，据答推断：讲讲你做的这个重写拆分模块）

**我：** 那我目前我们这个问题的重写拆分的话，其实主要就是有**三层的一个结构**。

第一（项）其实就是……因为我们先讲一下我们做这个重写拆分的**原因**，就是我们用户问了一些问题呢，和我们拿去检索的一个查询其实不一定是强相关的。比如说用户有一些**口语的照应**啊，或者**术语对齐**，那么我们还有的话就（是）**多轮对话之后我们会省略某些代词**啊之类的。那么这三类如果不处理好的话，那么我们的 **embedding** 还有就是**向量检索**，还有 **BM25** 的这个检索的话都会被**污染**。所以我们就做了这一件就是问题的重写拆分这个工作。

那么我们主要就是分为了以下几件事去做：

**第一个**我们是做一个**术语的归一**，去做一个确定性的规则，那这些规则会存在一个我们一个**规则表**里面，（由）程序加载。那如果用户这个输入进来的时候呢，我们会先按这个（规则）进行一个匹配，[避免就是短期去截取这些常识（原意不明）]，然后把它替换掉，我们就（不做归一）。

那**第二层**就是我们做一个……第二层是我们的一个**主力层**，其实就是我们使用模型去给我们的这个用户提交这个问题进行改写拆分，那么会出 **N 个问题**。然后的话我们会通过这个 **prompt**，还有 **temperature** 去控制它、控制模型的输出，我们需要严格输出这个 **JSON 的格式**。

然后的话**第三层**，第三层就是我们就是做了一个**规则的拆分兜底**。那么如果大模型解析失败的话，我们就会走规则去按**标点**分，然后我们就会拆分到 N 个子问题。然后我们会提交给**线程池**，让线程（池）我们意图识别的一个**专用的线程**（池），[只要现在（让它）持续并发执行]这个意图的识别。目前我们是这么做的。

其实我们的目的就是想要我们检索的时候呢，这个问题干净一点。所以的话我们会先走这个问题的改写，改写成我们（在）走检索的时候的一个能够匹配到相关的一些向量，是这个改写的目的是这样子。

### Q4 意图识别：做哪几件事？为什么要并行？

**面试官问：**（问句未录清，据答推断：你的意图识别模块做了什么）

**我：** 那我们意图识别，那其实是因为我们需要去……那么我们意图识别需要去做一件事，就是我们会去**定位到具体的一个知识库**。然后第二我们会做三件事：第一个就是我们会**定位到具体知识库**，第二个就（是判断）**是否需要调用 MCP 的工具**，就是（判断）我们识别是否需要去走系统的自答，避免后续的一个**大模型**的一个调用。其实（工具调用）是可以和 MCP 工具的（调用），其实是可以执行的——如果一个问题进来，他又有某一个意图，又需要调知识库，又需要去调 MCP 的话，我们会分别去**并行执行**拿到结果。这是我们这两个功能的一个区别。

### Q5 追问：改写的 prompt 是怎么设计的？

**面试官问：**（问句未录清，据答推断：改写拆分的 prompt 怎么写的）

**我：** 其实这个改写的这个 **prompt** 的话，其实就是我们会写好这个**角色**，定好他这个任务，其实他（的角色）是一个**问题的改写与拆分的这一个专家**。那么然后我们会给他定一个**拆分的规则**，那么我们比如说你需要去按照我们的这个规则去进行一个拆分。

然后的话我们会**调参**，我们会将这个参数去**严格控制到低温的输出**，尽量让它输出一个完整的这种格式，这也是我们要做的一个东西。

其实就是说比如说我们就是会有一些具体的一些规则，具体的一个拆分的规则，比如说「**系统 A 怎么用，然后系统 B 怎么用**」，那这种我们会给一个**显式的举例**，比如说他就会拆成「系统 A 怎么用」「系统 B 怎么用」；**一些不拆的例子**的话，我们也会给到他，比如说「**A 跟 B 有什么区别**」，这种我们就会**不拆**。然后的话是目前的话我们是这么做的。

### Q6 项目深挖：MCP 服务升级——为什么要加"写"能力？怎么控制权限？

**面试官问：**（问句未录清，据答推断：为什么要升级 MCP 服务）

**我：** 因为我们目前的系统（做问答）不涉及这些流程，所以我们目前就是希望它（Agent）接入这个、让它有自主的一个思考。所以我们就是将 **MCP 的一个服务进行升级**。原本这个 MCP 服务的话其实是**只有查询的这些功能**，那么我们现在就是升级后呢，我们会**增加一些写的功能**。那么写的功能，我们是会觉得是**比较危险的一个操作**，所以我们是在这一套流程中是做了一套升级。然后我们其实**重点是管这个写的操作**。我目前升级 MCP 服务其实就是为了操作这个权限，去控制住它。且（操作）这个权限——

那 MCP 这个服务呢，我们是我们的架构其实是分为了**三层**。其实首先它是一个**独立的一个服务**，我们调 MCP 服务的时候会走一个**端口**（区域）进行一个调用，带上他的这些的 **ID**，还有他的一些**描述**进行这个工具的调用。那么我们是**强制分了一个读写**，因为我们刚刚说我们升级的嘛，那我们强制分读写的话，在这个服务在**启动的时候**，我们会强制让这些工具**上报**，他是**读类型还是写类型**，如果**不报**的话，我们会进行一个**报错让这个服务起不来**。因为如果他不报读操作或者写的（类型），我们会认为这是一个比较危险的一个行为，所以我们就干脆让服务起不来。服务起不来的话，因为他是一个**单独的服务**，所以也不会影响我们**主流程**的一个使用，只是说 **MCP 工具调不了**而已。

然后**第三层**的话就是我们这个操作的一个具体的细分操作。我们是因为我们是接入了 [真是够本（名未录清）]，所以我们**读操作**的话其实是会继承了一个接口，然后的话改写了它的这个**权限的一个判断**。那然后我们判断为**读操作**的时候呢，我们就会放心让他去读；那如果是判断为**写操作**的时候呢，我们会通过这个 [DVR] 就是 **AOP**，它一个**洋葱（模型）**，相当于理解来说的话，其实相当于就是 **AOP 的面向切面编程**。在**调这个写**的时候呢，我们会强制让这个调用的这个流程的这个**状态挂起来**，然后**落到库里面**，就是我们将这个的状态设置为**等待的一个状态**（等待用户的一个状态？），然后落到库里面，然后**等待这个用户确认**。然后我们会等待用户确认之后呢，我们会有一个[参数改]的一个操作，然后用户确认以后我们就会将这个再重新从库里面拿出来，将这个结果拿出来，然后再进行一个 MCP 工具的调用。目前我们的操作是这样，我们的一个功能的一个流转就是这样子。

然后我再补充一点，就是说在这个 MCP 写工具调的过程中呢，我们还会把——目前目前就是这么多。（**补充未说完即收住**）

### Q7 项目深挖：分布式限流怎么做？

**面试官问：**（问句未录清，据答推断：你们怎么做限流）

**我：** 因为做限流的话，因为我们的调用量其实在系统里面的调用量其实也算还可以，所以的话每个（我们）有 1000 多个用户，如果同时请求进来的话，其实是很容易将模型的一个 **GPU 的这个资源利用率**给**打爆**的。所以我们的话就是使用，就是我们希望在 **Redis** 这里做一层**限流**。

然后的话因为我们考虑过一个方案，就是如果我们做一个限流方案，那么如果我们只在**最外层**进行一个限流的话，其实他那些流量进去之后还是会不断地打到 **GPU** 那里。所以我们希望通过一个 **Redis 信号量**的一个机制去分配，每台实例分配到（这个）信号量，然后让它去做一个 [均衡（原意不明）]。

那么我们目前的一个架构（是）：请求进来的时候，我们会进行一个**入队**。入队之后，我们会按照——我们使用的是 **请求类型的一个值的字段**去作为一个分数进行一个**排队**，然后我们**先进先出**。

然后那么我们刚刚说的这个**信号量**的话，其实是在我们[外层、做原子（操作）]的时候，我们会做一个信号量的一个判断，判断这一个请求是否在这个**信号量的这个窗口内**。

然后因为我们还会（判断），因为我们考虑到还有**一些僵尸的一个进程**，他可能是——就是我们请求进来说我们会给他分配一个**存活时间**，所以的话他有可能会**过期**，所以我们会顺便**清理这些僵尸的进程**，然后进行**出队**。

出队之后，我们会做一个**状态机**，然后让这些请求去**抢占**这一个——抢占这个信号量，是做一个**原子抢占**的。那么抢占之后，我们会从**等待抢占**过渡到**授予抢占**，然后我们才会去调用大模型。调用结束之后，如果我们的**信号量**的这个数量已经没了，那么我们会进行一个**等待**。等待的时候我们会当**一个实例释放**他的这个信号量的时候，我们会通过进行一个**广播**，让**每一个实例**去进行抢占，然后的话重新抢占他们的这个**信号量**。那么我们目前其实是这么做的。

（**Token**）消耗情况、这个计算情况我们是有做，但是**我没有负责这块**。不过我们会有具体的一些观测指标，就是我们搭建那个**评测集**嘛。

### Q8 项目深挖：评测体系怎么搭的？有哪些指标？

**面试官问：**（问句未录清，据答推断：你们评测怎么做的 / 有哪些指标；面试官此前问过"首字延迟"）

**我：** 我们会有，你刚刚说到了这个**首字（延迟）**，就是首字延迟。我们这个首字延迟定义的**标准**就是：在大模型**不算思考**的情况下，在大模型**吐出第一个字**的时候，我们就会算他的一个——就是**请求进来**到**吐出第一个字**的时候，我们就是算他一个**首字延迟**。

那么我们还有一些我们**自己自建的指标**，比如说**意图识别的准确率**。这个意图识别，我们会分为**三类**，就是我们（在做）意图识别的时候，我们是有**三个意图节点**，就是**三类节点**，我们判断这**三类节点的意图识别准确率**。

然后我们还会观测这个**召回的 Top**，还有这个**精确度**。

还有就是我们**第二类的观测指标**，就是我们会使用 **[EMS（名未录清，疑为 RAGAS）]** 这个公共的这个**开源的这个组件**去搭建我们这个评测集。有「**答案相关性**」「**答案正确性（是否题）**」，然后「**上下文精确度**」还有「**上下文召回率**」这四个指标，我们会进行一个观测。

产生情况的时候，因为我们其实是这样子的，因为我们去做这个系统，我们需要有一些指标去评判我们这个系统做得好不好嘛，所以我们就会产生有做这个评测的这个想法。所以的话我们就会去做一些**工程类的指标**。所以（我们这个）评测集的是**我跟我的架构师去进行搭建的**。

然后我们会首先就分为**两类指标**：
- **第一类就是工程类的指标**，比如说的**（首字）延迟**啊、**存储量**、（意）图识别的准确率，然后召回的这些**分块的准确率**，然后召回的 **[Top-K]**，然后这些指标我们来进行一个评判。
- **第二类就是我们用开源的一个指标**去判断这个我们召回我们检索的一个检索是不是能够产生一些比较好的效果，所以我们就用 **[LOIS（名未录清，疑为 RAGAS）] 工具**去进行一个评判。

目前是这样（子）。

### Q9 项目深挖：存储选型——为什么用 PgVector 不用 Milvus？

**面试官问：**（问句未录清，据答推断：向量库为什么选这个）

**我：** 这个**技术选型**的时候，我们就是考虑到——我们其实就对比了。目前来说，我们的**性能还没需要用上 Milvus**，所以的话我们是**暂时先用了这个 PgVector**。

因为 **PG 的话是我们内部可以使用的数据库**，（Milvus）我们去使用的时候可能还是需要去**向上级报备**。因为我们是**金融公司**，所以**监管的这个要求**下来的话，我们是不一定能够使用到这个数据库的。所以**为了兼容内部的一些使用情况，我们就会使用 PG**。

然后的话是这样子，然后我们会考虑到，因为我们系统我们本身**现有维护的系统也是使用 PG 这个库的**，所以我们觉得**接入这个是比较轻松一点**，就不需要太多的一些架构、上线以后怎么去持续去运营的。

### Q10 项目深挖：系统怎么迭代？

**面试官问：**（问句未录清，据答推断：系统后续怎么迭代）

**我：** 其实（就是）技术的一个**技术架构升级**（嘛）。刚讲到我们会往——我们现在其实是在往这个（方向）进行一个**架构升级**。

然后**第二类**的话其实是我们会就是（评测集），我们会**定期的去跑一个评测**，我们**每个版本都会跑一次这个评测集**，然后的话去监控一下这个指标是否正常，然后如果指标异常情况下，我们会去考虑一下我们**哪一个模块是做得有问题的**，我们会去进行优化。目前这两类的一个迭代。

然后我们在设计的时候，我们会**线上采一些真实的用户的 case 回来**，进行一个**回流到我们的评测数据集**，然后**逐步过渡到真实的数据集**，然后去进行优化。

### Q11 非技术：团队规模与分工

**面试官问：**（问句未录清，据答推断：团队多大、怎么分工）

**我：** 我们团队的话目前是有 **20 人左右**，其实是有**前端**（客户端），然后还有**算法**。我们这个组就是我们这个组的这个事情就是 20 人。然后算法的话主要是做一些**模型的一些部署**，然后**模型的一些微调**。那我们做的就是**工程类的事情**，相当于就是说**使用这个大模型来构建起来我们这个体系**。

**前端 10 个**，然后**算法的话是 5 个**，（还有）**产品**。其实产品我们产品的话是**分属另一个团队的另一个组**里面，他会给我们去需求，然后我们去评估。

### Q12 非技术：AI 开发工具的使用 / 团队怎么用 AI

**面试官问：**（问句未录清，据答推断：你们日常用 AI 开发工具吗 / 团队怎么用 AI）

**我：** 我们日常用了 AI 开发工具的话，其实是 **Claude**，然后我们接的是**国内的模型**。在外部自己用的话，其实我们对比过 **OpenAI** 的。其实我们觉得产生了一些结果的话，我们没有详细指标嘛，但是我们感觉起来的话就是 **Claude 是要更好用一点的**。然后 **GPT** 的话其实稍微输出了一些内容可能不太满意，需要去**重复修改**。

（问面试官/自述）模式，有什么样的一些调整？跟传统的那些——我的理解是说，**产品**其实它可以用 **AI** 去画一些更多的**原型图**了嘛，就是会画得更精美一些，然后交给前端，要交给我们，然后我们去评估。那么我们首先产品的话就是**会用 AI 去做这些功能的原型**了，已经是。然后的话**前端**的话就是拿到这个**原型**之后，他会根据——我觉得前端的话，就是会根据这个功能原型去将这个代码进行一个调整，然后去评估嘛。

那我们的话其实更多的还是去**关注整个设计的一个体系能不能支撑这么多流量**，然后到底有多少个用户，然后我们的**每一层应该怎么做**，去根据 AI 去（出）一些方案让我们去选择。

然后**测试**的话，其实就是根据我们根据产品这个文档，我觉得可以去加速，就提升他的这个测试，还有**用例的一些提效**。

### Q13 非技术：我的期望（想做什么样的事 / 公司氛围）

**面试官问：**（问句未录清，据答推断：你对岗位/公司有什么期待）

**我：** 我觉得这样子对于这个项目其实更倾向于项目来的，我更倾向于就是做一些**更加新颖的一些事情**。然后的话其实像您刚刚介绍的你们的这个**工作手机**，其实我觉得也是比较有意思的事情。我觉得是我目标来说是倾向于做——我目标是我认为比较有意思的事情的话，其实我觉得（对项目类型）没有太大的要求。因为现在其实要求来说的话，我可能是对**公司一个氛围**会更感兴趣嘛，希望有一个就是比较有**创新**的一个氛围，或者说比较**融洽**的一个氛围，就是**好沟通**的一个团队。

### Q14 非技术：工作时长

**面试官问：**（是指下班时间还是总体的呢？）

**我：** 目前其实大概就是**七到八个小时、7.5 到 8.5 的一个工作时长**嘛。其实我们现在是 **8:30**（上班），然后正常来说的话，下班时间是 **17:40**，然后一般是 **18:00 到 19:00** 左右这个时间下班。有时候项目赶的时候就会晚一点，可能 **19:30 或 20:00** 这样子。

### Q15 非技术：待遇 / 薪资（**角色不明**）

**面试官问：** 待遇是吗？

**我：** 对我这方面的期待，还是说我自己的薪资呢？
**我：** 问一下有没有年终呢？
**（角色不明）** 六到 40 这个区间吗？
**（角色不明）** 25 到 30 的话，我现在就是现在的三十多。其实是要看绩效，保底的话就 20-25。
**（角色不明）** [10 25 分钟（原意不明）]

> ⚠️ 本段为混录且语序错乱，**分不清哪句是面试官、哪句是我**，按「角色不明」原样保留，不代为归位。

### Q16 我的反问

**我：** 想问一下，你平常**下班会干嘛**呢？
**（角色不明）** [的小伙伴居然是 9:00 的，居然都是 4:30 到 10:00（原意不明）]

**我：** 我想请教一下，就是说我**进入这个岗位的话，需要有哪些能力**去才能胜任？技术（能力），对业务的理解——相当于企业的研发。现在方便请教一下，就是这个**产出**的话就是你们的这个**业绩**——就是公司的业绩，不是说您的业绩，公司的一个业绩是靠什么去产出？是靠这个 **AI 手机**产出，还是说还有其他的一个对外的**增值服务**呢？

**面试官答：** 收入的话，当前主要是**卖手机**和这个 **SaaS 软件服务收费**。

**我：** 感谢您的时间。
