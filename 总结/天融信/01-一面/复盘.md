# 一面复盘 · 天融信

> 岗位：研发（AI + Java，安全运营方向）
> 日期：2026-08-31
> 面试官：主面试官（AI/项目/工作期望）、Java 面试官（八股）、补充面试官
> 原文：[原文/面试实录.md](原文/面试实录.md) · 原始转写：[原文/2026-08-31-天融信-transcript.txt](原文/2026-08-31-天融信-transcript.txt)

## 问了什么

1. 自我介绍
2. RAG 项目解决什么业务问题、由哪几个模块组成
3. 多路检索引擎的具体实现
4. 会话记忆：滑动窗口 + 摘要压缩策略怎么定的、会不会丢信息
5. MCP 协议、工具调用、参数提取怎么实现
6. Java：`==` 和 `equals` 区别；HashMap 初始容量与扩容；CAS 原理、解决什么问题
7. 线程池参数 + 拒绝策略在项目里的实际应用
8. 多路召回用线程池，怎么确保等到多路结果
9. pgvector 和 Milvus 的区别
10. 提示词工程怎么做
11. 反问：工作期望、公司业务、团队规模、结果反馈时间、岗位能力要求

## 卡在哪

- **自我介绍只有 ~40 秒、没重点**，"然后的话"口头禅密集，"Java 开发工程师"口误成"早晚开放工程师"，没带任何数据亮点。
- **`==` 和 `equals` 答错**：把 equals 说成"比较哈希值"，和 hashCode 混淆了。
- **HashMap 初始容量说成 8**（默认是 16；负载因子 0.75 说对了）。
- **MCP 答得太薄**：只讲了"意图识别阶段划分 + 函数描述"，LLM 提参三态、garbage 不进参、多工具并行这些逐字稿里的核心亮点一个都没抛。
- **会话记忆被追问"会不会丢信息"时答得绕**，没主动抛"合并式摘要防遗忘 + 冲突以本轮为准"。
- **线程池参数现场编**：临时说"核心=CPU 数、最大=2 倍 CPU"，没落到项目真实参数，IO 密集这个配置也偏保守。
- **Milvus 口误成 "miniverse"**。
- 反问环节"结果什么时候出"问了两次，略显重复。

## 下次怎么答

- 自我介绍背 **1 分钟版**，带数据（多路检索 Top-1 命中 +30.5%、首字响应 6s 内），先练到脱稿。
- `==`/`equals`/`hashCode`、HashMap 初始容量与扩容、CAS、线程池 7 参数——见「高频错题」，考前必背。
- MCP 一定主动抛 **LLM 提参三态 + garbage 永不进工具入参**；会话记忆主动抛 **合并式摘要 + 冲突以本轮为准**。
- 线程池参数落到项目真实线程池名（`ragRetrievalExecutor` / `mcpBatchExecutor` / `memorySummaryExecutor` 等），别现场编。

## 要补的点

- 生成「面试题目/八股基础/Java集合与并发.md」（`==`/equals/hashCode、HashMap、CAS、线程池）。
- 补齐各逐字稿「待补点」里的**线程池参数**（README 里已列，仍未补）。
- 自我介绍 1 分钟版补具体产出数据。
