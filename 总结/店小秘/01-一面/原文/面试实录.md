# 面试实录 · 店小秘 一面

> 岗位：Java 后端开发（推断）
> 日期：2026-09-16
> 原始转写：[2026-09-16-店小秘-transcript.txt](2026-09-16-店小秘-transcript.txt)

## ⚠️ 还原说明

- **本转写为单轨混录**，且**面试官的问句绝大部分缺失**——整段转写几乎是「我」方连续输出，面试官只在开场确认声音、以及结尾处留下可辨识语句。
- 实录中「**面试官问**」凡能听到原文的**照引**；问句缺失的用 `【问句未录清·据答推断】` 标主题，**不保证逐字**。
- 我的答句按语义断句、加标点，**词序与用词尽量不改变**；确定无疑的语音错字在原位用 `[还原]` 标出（列表见下）；答错、口误、卡壳、**自己说"了解不是很多"的地方一律原样保留**。
- 本场的语音识别错字**密度极高**，且同一术语出现多种误识（如「线程池」→ 性能尺码 / 县城池 / 性能素 / 持续）——还原表按「首次出现」归类。
- **结尾一段为面试官反馈**，但转写严重失真、角色难辨，按「**（角色不明）**」原样保留，不代为归位。

### 主要错字/语音识别还原表

| 转写原文（音） | 还原 |
|---|---|
| IG（相关系统 / 知识问答平台 / 层） | **RAG** |
| 项目上资金 | 项目上的**话**（疑似） |
| 费用量 | **调用量** |
| 千百多个 | **千多个**（原意不明，疑为 1100 多个） |
| 多录的照检索与召回 | **多路**（检索与召回） |
| 绘画 / 陈J瓦 | **会话** |
| 招把 | **Java** |
| 构件 | **构建** |
| 艺术识别 | **意图识别** |
| NCP | **MCP** |
| 启动启动模块 / 装模块 | （名未录清，疑为**接入 / 网关**模块） |
| 一个型的一个 | （名未录清，疑为**一层**） |
| 大型的这个层 | **大模型**这一层 |
| 活动窗口 | **滑动窗口** |
| 药 / 炸药 | **摘要** |
| scoop | （名未录清，疑为 **Spring / SpringBoot**） |
| 这口 | 这**块**（疑似） |
| 预知 | **阈值** |
| 邮标 | **游标** |
| 九八人的一个消息 | （原意不明，疑指轮次 / 消息条数） |
| 开始集合 | 开始（指针） |
| 砝码 | （原意不明） |
| 筷子针 | **快指针** |
| 子针 | **慢指针** |
| 注视进行或者挂起 | **阻塞**或者挂起 |
| ft | **FutureTask**（疑似） |
| 追求这个接口 | **RunnableFuture** 接口（疑似） |
| 配合性能持续（使用 / 执行） | 配合**线程池** |
| 创新执行 | **异步执行** |
| 那个实验的任务 | （原意不明，疑为"最慢的那个任务"） |
| 和并 | **合并** |
| Compton | （名未录清，原意不明） |
| CS | **CAS** |
| 比较清亮 | 比较**轻量** |
| 那个理解质量 | （原意不明） |
| 性能尺码 / 县城池 / 县城 / 性能素 | **线程池 / 线程** |
| 画师 | **时候** |
| CPU 的数 / 合数 | CPU 的**核数** |
| 最大GP | （名未录清，疑为"最大"） |
| 形成n次同步（同步无缓冲） | **SynchronousQueue**（同步无缓冲） |
| 主持队列指数对 | **阻塞队列** |
| 非核新县城 | **非核心线程** |
| 邀后 | **然后**（疑似） |
| B压 | **背压** |
| 外省 | **外层** |
| wide（信号量机制） | **Redis** |
| 事实 | **10**（疑似） |
| 实力 | **实例** |
| 指令（这个队列） | **指定**（疑似） |
| 无解的 | （原意不明，疑指"无界的"） |
| P / P搜索 / Paget | **PG / PgVector** |
| 项链 | **向量** |
| 5.7六 | （版本号，原意不明） |
| 毕加速 | **B+ 树** |
| 多路平衡查找数 | **多路平衡查找树** |
| 逼数 | **B 树** |
| 数的高度 | **树的高度** |
| 分布分表 / 分布分秒 / 剖部分表 | **分库分表** |
| 所以（的条目 / 的查询效率） | **索引** |
| 四成数三乘数 | （原意不明，疑为"三层 / 四层"） |
| 覆盖一天班考试 | （问句未录清） |
| 与方程的分数 | （原意不明） |
| st 表 | **student 表**（疑似） |
| set（这个名称） | **select**（疑似） |
| Avi | **AVG** |
| 反恶魔的优惠券系统 | **防黄牛 / 防薅羊毛**（疑似，原意不明） |
| 树液结合实际 | （原意不明） |
| 我叫彭世峰 | 我叫 **xxx** — 全库规范：真实人名一律以 xxx 代替 |

---

## 面试官问 / 我答

### Q1 开场问候 + 自我介绍

**面试官问：** 你好，听得到吗？

**我：** 老师你好，我叫xxx，我毕业于新疆大学软件工程专业，然后我目前在中国平安人寿保险股份有限公司担任开发工程师。

那我们目前的一个项目其实是做 **RAG** 相关的一个系统，其实是我们寿险内部、我们公司内部的 **RAG 知识问答平台**。然后的话项目上的话呢，其实是保持了**日均 50,000 的一个调用量**，然后服务了活跃用户的话其实是**千多个**。

那么我在这项目中深度参与的内容，其实有**意图识别**，然后**多路的检索与召回**，然后**会话的记忆机制**，还有**分布式**（限流），还有一个**（评测）搭建**。

那然后我们的项目（技术栈）其实是使用 **Java** 进行一个构建的，然后分为了**四个层级**。那么整个项目的话有一个（接入）模块，我们这些模块其实是跟用户的输入有一些功能——检索这些功能，然后意图识别这些功能。然后的话第二层的话其实就是一个（层），然后的话有 **MCP 层**还有我们的 **RAG 层**，目前正在升级这个层。然后底层的话就是我们的**数据库**，还有中间这个其实还有一层就是我们**大模型**的（接入）层。那我目前的这个项目的架构讲的就是这样子，然后我的自我介绍完毕。

### Q2 追问：项目时长 / 你主要负责的是会话记忆吗

**面试官问：**（此段与我的答句混录，可辨识部分）这个项目其实做了大概八个月左右？……做一些（工具 / 自己内部的系统）是有（确定的）？……我的理解来说，您目前在这个项目（里）主要负责的是**会话记忆**的一个功能吗？

**我：**（接 Q3 作答）

### Q3 项目深挖：会话记忆的整体流程

**面试官问：**（问句未录清，据答推断：讲讲你的会话记忆）

**我：** 其实我们整个**会话记忆**的流程来说的话，其实就是用户请求进来的时候，我们就会去**并行地做两件事情**，其实就是（并行）去做两件事情：一个是我们的（会话历史）的（拉取），一个就是我们的**记忆摘要的拉取**，就两件事。

然后目前的话您说的刚刚那个**滑动窗口**，其实是我们这个记忆会话机制，我们目前是采用**摘要 + 滑动窗口**的一个形式去构建这个（机制）。那然后我们的滑动窗口其实是因为我们有一个问题，就是我们**上下文的话其实是不能够满足**，就是上下文不可能非常大嘛，所以我们就定了一个窗口来去滑动，然后去获取这个用户的问题、还有我们（模型）讲的一些回答。所以的话，然后我们**之前丢掉的那些窗口的那些信息我们会通过摘要的形式补回来**。然后的话我们目前的话其实就是这样子一个操作。

我们是 Java 技术占加上这个 [scoop（名未录清，疑为 Spring/SpringBoot）]，其实也是 Java 的一个技术体系，然后目前我们是这么搭建的。

### Q4 追问：滑动窗口是怎么定的

**面试官问：**（问句未录清，据答推断：窗口大小怎么定的）

**我：** 然后我们这块其实就是我们**定位八轮的一个滑动窗口**，然后我们每一轮的窗口其实就是**包含了两条信息**：一条是用户的一个问题，另一条就是一整个回答。

那么，然后我们定了一个**阈值**之后，我们会有个**游标**。我们游标其实就是——想要怎么说，举个例子，就是比如说我们有（八 / 九）轮的一个消息，那现在我们的窗口其实就是有八，然后的话我们到**第九轮**的时候，其实我们**第一轮的数据**、载进来的用户一个提问就已经丢掉了。

其实我们这个是滑动窗口，其实是**配合着摘要去进行一个设计**的。那我们这个到八轮，然后我们从**一到八轮滑到了二到九轮**，所以的话我们第一轮的这个信息，其实我们是想保留的，所以的话我们就**通过这信息提交给大模型，然后让他给我们输出一个（摘要）**，然后的话我们再将这个**摘要**跟我们这个**窗口内的这个会话的信息提交给大模型**，然后让大模型进行输出。是这样子的一个设计。

### Q5 追问：滑动窗口的代码怎么实现

**面试官问：**（问句未录清，据答推断：代码层面的滑动窗口怎么实现的）

**我：** 代码（实现）滑动窗口的话，我的理解就是，我们实现这个滑动窗口的时候其实我们会定一个**开始（指针）**，有一个**结束（指针）**——开始、有结束（指针）嘛。那我们就按照我们固定的一个（步长）进行一个滑动，**固定一个轮次进行一个增加**。比如说我们定了一个 A 定了一个……等于 8，所以的话就是我们定了中间——中间的那个窗的话，其实就是**从开始的话，其实就是 7+8**，我们就是这么一个顺序，然后我们每一轮、每进来一个问题，那我们（指针）就会加，然后去取这个值。我们是这么设计的。

### Q6 追问：双指针 / 快慢指针

**面试官问：**（问句未录清，据答推断：能不能用双指针 / 快慢指针来做）

**我：** 这个我理解，其实我的想法就是可以用**双指针快慢指针**去判断这个 [砝码（原意不明）]。其实我们可以定一个——一般来说的话我的理解来说，就是**快指针一次（走）两步，慢指针一次走一步**，然后的话去判断有没有的话，其实就是**他们俩快慢指针会相遇，那么就是有（环）；如果是没有相遇的话，那么就是没有**。我的理解这样吗？就是我一个人在走路，然后我在跑步，我跑得比他快的话，我肯定会追上他，其实就是这个意思。如果能追上他的话，那我肯定跟他有相遇嘛。

就是比较形象化比喻的话，在一个有环形那个操场上跑步。

### Q7 追问：FutureTask / 异步任务的底层

**面试官问：** 听得到，那它的底层（是怎么实现的）？

**我：** 它的底层的话，我**就是了解的不是很多，但是我可以尝试一下**，就是说它底层其实配合是（线程池）进行使用的，然后 [ft（名未录清，疑为 FutureTask）] 的话，其实是**继承了一个应该是继承的 [追求（疑为 RunnableFuture）] 这个接口**吧，然后底层的话其实就是配合（线程池），那么它每一（步）进来一个任务的话就会**提交给线程池进行执行**嘛。执行之后如果它需要**异步执行**的话，那就是异步地去，然后它会中间有个 **get 的方法**，它可以去、当任务没做完，它可以去**阻塞或者挂起**嘛。

然后的话如果是异步执行的话，那就可以用（Future）的形式去将这两个任务去，就是（取）这两个任务的一个——**就取这个两个任务（里）最慢的那个任务**进行一个**合并**的理解、控制这个状态。其实我觉得就是应该是有一种有状态的一个形式去控制这个状态吧，就是比如它有这个，比如说它有这个**进行中**的一个状态，或者**执行中**，然后**执行完成**的状态。如果它执行完成的时候，那么就是 [Compton（名未录清）] 的话，就是相当于就等待这个任务被拿到，然后合并吗？那然后它这个状态其实应该也是要**配合这个 CAS** 来做的，因为它可能会有 CAS 的话，它这个比较（轻量）一点嘛，它这个锁相当于就是这个锁的话会比较**轻量**一点，所以的话是能够提升一些性能。

### Q8 项目深挖：线程池参数与执行流程

**面试官问：**（问句未录清，据答推断：你们线程池怎么配的）

**我：** 其实我们对于我们（项目里）的模块，就是比如说**意图识别**，然后**多路的（检索）召回**，我们都会有一个**专有的线程池**。

然后我们目前评估这个**线程池**的一个使用的话，其实就是我们目前**调用大模型的时候，是一个 IO 密集型的调用，它本身不会特别占用 CPU 的资源**，所以我们是对这个（线）的一些参数进行设置。比如说核心是，我们定了 **CPU 的数 × 2**；然后最大……（又说）我们核心的数就是 **CPU 核数**，然后最大线程数的话就是 **CPU 核数 × 2**。然后我们配合着我们的**拒绝策略**，还有我们这个**排队的一个策略**：我们排队策略是 **[SynchronousQueue] 同步、同步无缓冲**的这个，然后拒绝策略的话，我们就是会**让它去进行执行**，是这样子，就是**不丢掉任务、继续执行**（比较少）。

（面试官追问：线程池提交任务时会怎么执行？）县城……就是说就是我提交（任务）的时候它会怎么执行这个任务吗？——其实就是的话，它会判断这个**核心线程数有没有满**嘛，如果没满的话就去提交；满了的话，如果满了的话就会尝试**放进这个阻塞队列里面**，然后如果队列还能传下的话，那就去继续排队；如果是队列都满了的情况下，就会去考虑**是否可以新建这个非核心线程**，就是最大的线程数嘛；如果最大的数满的时候，我们就会**触发这个拒绝策略**。我理解来说就是这么执行的流程。

我们项目里面的这个其实就是**用同步的（队列）去执行的**。然后我们评估的话，其实就是我们希望我们的这些问题的**结果都能反馈回来**嘛，所以我们就会让、我们就会让这个——如果是核心线程满的情况下，我们会选择**让主线程也去进行同步调用**，然后**让结果都返回**，[邀后（疑为"然后"）]形成一个**背压**，这样的话我们也好去调优。我们就是这么做的。

### Q9 项目深挖：分布式限流

**面试官问：**（问句未录清，据答推断：你们怎么做限流）

**我：** 目前（因为）我们在、因为我们这个项目使用的时候呢，其实是在**外层**的话，我们会使用一个**信号量机（制）**，这就是 **Redis 信号量机制**，我们会**控制这个模型调用并发**嘛。

所以我们是目前的这个**信号量是 [10]**，所以我们觉得就是对每一个**实例**来说，因为我们有**四个实例**嘛，其实是每一个实例能有那一个并发，其实**大概每秒就是二左右**，所以我们就设置为**比他大三倍左右的一个队列**，我们觉得是比较合适的。

其实这样子我觉得，如果**没有指定这个队列（大小）**的话，可能会**触发内存溢出**吧，就是因为你没指定这个、没指定这个（队列）的话就不断地提交、提交给这个线程池去执行嘛。然后又因为（队列）其实是**默认（无界）**的，所以的话有可能会触发这个风险。

### Q10 项目深挖：存储选型（PG / PgVector）

**面试官问：**（问句未录清，据答推断：你们向量库怎么选的 / 用的什么数据库）

**我：** 我们目前用的是 **PG**……跟 PG 差不多吧。然后的话目前我们就是因为这个，所以才用的 **PgVector**。我们就是调大模型的时候，我们用 **PgVector** 的话，就是想、想（配合）大模型，然后**存储他们的这个向量**嘛，所以的话我们会选择用 PG。因为的话目前是**不支持这种（向量）的形式**的嘛，我们用的是——我们目前使用的是公司内部目前使用的是这个 **[5.7 / 6]** 吗？然后的话其实**不支持这个向量的形式**了。

### Q11 八股：B+ 树与分库分表

**面试官问：**（问句未录清，据答推断：讲讲 B+ 树 / 为什么分库分表）

**我：** 结构的话，我的理解来说，就是它是 **B+ 树**的一个结构嘛，其实就是一个**多路平衡查找树**。

然后目前了解这么多，B+ 树的话，其实我的理解来说就是会跟 **B 树**来对比的嘛。**B 树的每个节点它会存，然后也会存完整的一些数据**，然后的话这样子的话会导致这个**树的高度可能会变高**；然后 **B+ 树**的话其实就是**非叶子节点它只存这个键还有指针，它就不会存真实的一个数据**，所以的话它在磁盘这个空间也会变小一点，然后就是**效率会更高**嘛。

其实我理解来说，从 B+ 树（角度）来**分库分表**，其实就是当**表数据量过大**的时候，其实 B 树跟 B+ 树它的树高度都会去上涨，上涨的话其实就是（每个节点能容纳的）条目就会不断增多，所以那种情况下查的时候就会比较慢。所以就希望**降低这个高度**来去做一个分表，然后分（表）要**降低这个树的一个高度**，然后去提高这个**索引的查询效率**吧。

其实我觉得这个应该比较简单，其实就是我们从顶层的这个节点下来，树高的话我们就会多查询——比如说有三层，比如说有四层的话，我们树变成了四层，那么我们就会**多查询一次**，假如我查询到底部的这个节点的话，我们就会多查询一次嘛。所以就是我理解这样子，所以就是大概了解。

### Q12 八股 / SQL：所有科目分数都大于 80 的学生

**面试官问：**（转写失真，可辨识部分）[覆盖一天班考试（原意不明）]，你手上只有一个数据（表），是这次考试成绩的……定义是**参与所有科目的分数都……大于 80 分**的这个人？

**我：** 想问一下就是他的科目是应该是有很多，就是这个同学应该参与的多个科目。其实我理解来说，这个就是 **student 表**，然后 **select** 这个名称，然后的话**对名称进行分组**，然后 **AVG 这个科目的分数**，然后**对这个分数取最小值，然后大于 80**。应该感觉应该是这样子。

### Q13 另一个项目：优惠券系统

**面试官问：**（问句未录清，据答推断：讲讲你另一个项目）

**我：** 这是我以前就是找工作的时候自己做的一个系统，其实就是写了一个**优惠券系统**，然后可以接入到那些商家品牌。然后我就重点就是写了一个这个 **（防黄牛 / 防薅羊毛）** 的优惠券系统，然后里面就是做了一些比较有亮点的事情，其实就是：**第一个是分库分表**，然后**第二个是做了一些设计模式**，**第三个就是对并发的、一个秒杀的一个缓存**，做了一些缓存那些设计。

我目前就是深度参与的这个项目，其实相当于是**从零（开始）第一个**吧。

### Q14 面试官反馈（角色不明，转写严重失真）

**（角色不明）：** ……你回答（的时候）比较（树液结合实际？原意不明），就得这么理解了，其实有些回答嘛……不是说的吗？我问你什么，我认为更准确的是哪一（种，原意不明）来着？能理解，就是说**不够专业**吗？其实。

---

## 附：我的反问

本场转写内**未见反问环节**（无「我问面试官」的可辨识语段）。
