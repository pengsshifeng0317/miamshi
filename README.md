# 面试资料 · 总索引

> **两级索引分工**
> - **一级（本文件）**：区块入口 + 熟练度状态。找「这稿能不能背 / 下次补哪」→ 改本表；状态以本表为**唯一真源**。
> - **二级（各区块 `README.md`）**：该区块「结构地图」——有哪些文件、各自一句话定位。找「这区块里有什么」→ 进对应区块 README。
> - 新增 / 移动文件：① 回本表登记或更新状态行，② 到所在区块 README 补一行链接与定位。
>
> 每完成一场面试练习，回来更新本表：熟练度、最后复习日期、待补点。
> 新建逐字稿时，套用底部「统一模板」。

## 📇 熟练度图例

| 标记 | 含义 |
|:--:|------|
| 🔴 | 没背熟 / 说不顺 |
| 🟡 | 能答但不顺，或需要提示 |
| 🟢 | 能脱稿讲，经得起追问 |

## 📂 目录结构

```text
miamshi/
├── README.md          # 一级索引 · 本文件：区块入口 + 熟练度状态（唯一真源）
├── 简历面试逐字稿/     # ① 跟简历绑定的逐字稿：自我介绍 / v1 模块深挖 / v2 升级对照
│   └── README.md      #   → 二级索引：本区块文件地图（每篇一句话定位）
├── 面试题目/          # ② 题目库：八股基础 + 场景设计题
│   └── README.md      #   → 二级索引
├── 总结/              # ③ 复盘沉淀：高频错题 + 按公司 / 轮次（复盘 + 原文）
│   └── README.md      #   → 二级索引
└── 提示词/            # ④ 辅助工具：角色扮演 / 逐字稿生成 / 整理复盘
    └── README.md      #   → 二级索引
```

---

## ✅ 复习进度表

### 🎤 自我介绍

| 文件 | 熟练度 | 最后复习 | 待补点 |
|------|:--:|:--:|------|
| [自我介绍.md](简历面试逐字稿/自我介绍.md) | 🔴 | 2026-09-16 | 补：3 分钟版正文待写；背顺 30s/1min（锚点 30.5% · 6 秒 · 5 万，录音去口头禅）；示例域口径与项目逐字稿对齐；**分工口径固定**——「深度参与」只列意图识别 / 改写拆分 / 多路检索 / 分布式限流，**评测与会话记忆一律说"轻度参与"**（长鑫 Q1 把评测顺进深度参与列举，前后场口径不一致）；⚠️ **店小秘 Q1 第二次违规（同一错法）**：会话记忆 + 评测又双双进了深度参与；**且 Q2 面试官据此问「您主要负责的是会话记忆一个功能吗」，你没纠正就顺着讲了 → 默认了被缩小的定位**。修法：① 开场句固定上述四模块；② **被误解当场纠正**「会话记忆我有参与设计实现，不过分工上我深度参与的是……」→ 纠正同时把话题拉回最强模块。⚠️ **架构讲碎（重复病）**：四个层级 + MCP / RAG / 大模型 / 数据库说得像在罗列，**仍缺 30 秒系统地图**（挂起待办第 1 条的第三次复发）；⚠️ **学历口径不一致**：华泰场说「研究生」，本场只说「专业」→ 统一。⚠️ Q14 面试官末句反馈是**「不够专业」**（转写失真但可辨）——本场病灶是「**术语全对、细节全错**」，不是表达问题 |

### 🎯 项目深挖 /（v1 逐字稿 · 按简历产出）

> 目录：`简历面试逐字稿/项目深挖/`。行尾标注：⚠️ 废弃旧稿 · 📚 参考（非逐字稿）· 🚧 待生成。文件清单与一句话定位见二级索引 [`简历面试逐字稿/README.md`](简历面试逐字稿/README.md)。

| 文件 | 熟练度 | 最后复习 | 待补点 |
|------|:--:|:--:|------|
| [意图识别树怎么做.md](简历面试逐字稿/项目深挖/意图识别树怎么做.md) | 🔴 | 2026-09-11 | 补：澄清 vs 退化兜底两场景分讲（低分<0.6 退全局、路径分叉歧义才澄清 FAST）、enterprise 模板节点数回源 **29**（旧 22/23 均过期）、意图总量全局≤3、打分输出不含 path（path 是输入）；⚠️ **降级原因：澄清第三次翻车**（长鑫 Q7 把**已废弃**的 `ambiguityScoreRatio=0.8` 当现行机制讲，还把 FAST 布尔确认说成"重新识别一次意图"）→ 必须按「**触发→判定→动作→兜底**」四段背成不可换词的链，**永久删掉 0.8 比值 / 0.7 规则**；⚠️ **长鑫 Q4 新缺口：90+ 口径答非所问**（问"这个数怎么来的"，答成 RAGAS 指标清单）→ 按「**定义**（意图 Top-1 节点命中率，**非回答正确率**）→ **怎么测**（标注样本跑 `intent-resolve` 看 Top-1 命中，`intent_pred == intent_l2`）→ **分母**（**触发澄清 / 无唯一期望意图的样本不计**，口径写死在脚本里）→ **评估集**（150 条覆盖全部二级意图 + `trap_type` 易混样本 + 按 l1/l2 分组切片；⚠️ **没有"三层各自准确率"**，评估集只有 l1/l2 两级标签）→ **与澄清联动**（敢给 Top-1 的都是高置信样本）」五步背成口播；**"覆盖 22 个二级意图"已过期，别报 22** |
| [多路检索怎么做.md](简历面试逐字稿/项目深挖/多路检索怎么做.md) | 🔴 | 2026-09-11 | 补：实现状态（4 通道实现齐全、默认只开向量、图谱 LightRAG/联网 You.com）、后置链组件名、命中率数字回源、入库链路块长 1024/overlap 128、线程池三池参数（意图 CPU/2×、检索 4×core=max、召回 2×/4×+LBQ100，CallerRuns）、（裁决 B：叙述统一寿险/重疾险域，示例不回改为企业域）；⚠️ **后置链（去重 → RRF → Rerank → EvidenceGate → MetadataEnrich）连续两场（小鹏、长鑫）整段没提**，必须进开场口播 |
| [问题重写与子问题拆分怎么做.md](简历面试逐字稿/项目深挖/问题重写与子问题拆分怎么做.md) | 🟡 | 2026-08-25 | 补：多轮历史为何限制 2 轮、意图总量 3 的硬约束 |
| [分布式限流怎么做.md](简历面试逐字稿/项目深挖/分布式限流怎么做.md) | 🟡 | 2026-09-16 | 补：lease 回源 600→yaml 30s（以 application.yaml 为准）、Lua queue_claim_atomic 原子抢占、Pub/Sub firing CAS 防风暴、拒绝走 SSE META→REJECT→FINISH→DONE、口径是全局并发 10 不是每秒 QPS、场景答题骨架（先现状[全局 10]后设计[压测目标]，勿把 30×4=120 当现值）；✅ 长鑫 Q15/Q18 为**全场最强段**（根因 + 竞态 + 医院叫号类比都对）；⚠️ 三处措辞待收口：CAS 说**本地 `AtomicBoolean`**（非 Redis 原子类）、防重复扫说 **`pendingNotifications` 计数**（非"只做加操作"，该机制不存在）、兜底说**每 ticket 各自 200ms 轮询**（非"后台全局定时任务"，且**信号量是全局的**无"单机信号量"），并补术语**惊群**；⚠️ **店小秘 Q9（同一错法第四次）**：信号量 **10 这次报对了**，但「我们有**四个实例**」和「设为**比他大三倍左右的队列**」是**现场编的**（`分布式限流怎么做.md` grep 实例/台/部署/副本 只有「多实例部署」，**全篇无任何数量**）。**决策：以后不报台数**，统一口播——「**单实例全局并发上限 10、排队最长 15 秒，超时降级为限流拒绝并落 REJECTED 消息**」 |
| [会话记忆怎么做.md](简历面试逐字稿/项目深挖/会话记忆怎么做.md) | 🔴 | 2026-09-16 | ⚠️ **店小秘 Q3/Q4/Q5 降级主因：连着三问全没答对，而逐字稿里全是写对的——典型「白准备的」**。① **Q3 摘要触发因果讲反**：说成"窗口滑出去之后再用摘要补回来"，真实是**重叠半窗**；② **Q4 单位错**：8 轮答对了，但没点出**单位是消息条数（8×2=16）**，也没抛**裁剪 / REJECTED 占配额**；③ **Q5 游标算法纯现场编**：说成"定开始指针 + 结束指针、固定步长滑动、7+8"——**代码里根本没有自增游标**。修法（背成一条不可换词的链）：**重叠半窗**（切点 `resolveSummaryCutoffId` 取**最新窗口正中间那条** `latestUserTurns.get((size-1)/2)`，只摘前半段，**只有这段重叠滑出窗口后才再次生成**，`JdbcConversationMemorySummaryService.java:139` 注释原话）→ **三个 message-id 指针**（`historyStartId` / `summaryCutoffId` `:276` / `afterId` 取自 `lastMessageId` `:254`；每次重新 `listLatestUserOnlyMessages(...,8)` 换算 `id > afterId AND id < cutoffId`，`ConversationGroupServiceImpl.java:47-79`）→ **收口句**「**不维护自增游标；用『最新 N 条用户消息的 id 区间』+『上一份摘要的 last_message_id』做幂等判断，天然抗重启和并发**」→ **双条件触发**（`countUserMessages >= summaryStartTurns(9)`；上一份 `lastMessageId` 仍 ≥ 窗口起点就 return）、**FAST 档**（temp 0.3 / topP 0.9 / thinking=false）、Redisson 锁 `ragent:memory:summary:lock:{userId}:{conversationId}`、落 `t_conversation_summary`。⚠️ **逐字稿两处已过期，本次核码才发现，务必回源改稿**：① 写「摘要 ≤**200** 字」→ 代码默认 200 但 **yaml 覆盖为 400**（`application.yaml:130`），**以 400 为准**；② Agent 层写「`triggerChars(20000)`」→ 真实 `context-window-chars` yaml 配 **100000**，派生**裁剪门 5 万 / 压缩门 8 万 / 保留段 2 万**（`application.yaml:51`）。另：补：摘要质量追问口径（单轮 query 测不到跨轮的评测边界+记忆评测三件套）、Agent 层字符水位压缩钩子、摘要模型 qwen-flash 走百炼云（非自部署）、8轮/9轮/前4轮的表述练顺；⚠️ **长鑫 Q12 两处与代码不符**：① **中间件只做注入**（`AgentUserMemoryMiddleware`），**提取在轮次结束后的后台 pipeline**（`AgentMemoryPipeline` + `AgentMemoryJudge` LLM 仲裁 ADD/SUPERSEDE/RETRACT/NOOP）——不是"每轮在前台提取"；② **Mem0 全仓无命中、属未接**，只能讲成"业界同类方案我了解过"。补：记忆三张表（`t_agent_memory` / `_extraction` / `_control`）、`AgentContextTrimmer` 六条设计；⚠️ **三层全景已升级**：以新增逐字稿《记忆系统的三层架构.md》为准（短期 / 中期 / 长期 = 其 Q8 / Q9 / Q0），口径由"短期+长期两层"改为「**载体借框架、治理自研**」+ **50%/80% 双水位接力** + 中间件顺序 `userMemory → compaction → confirm → skillMasking` |
| [MCP怎么做.md](简历面试逐字稿/项目深挖/MCP怎么做.md) | 🟡 | 2026-08-25 | ⚠️ 旧稿：对应旧代码（6 查询工具、无读写分层/确认卡/技能），复习以《MCP怎么做（新）.md》为准 |
| [RAGAS评测怎么做.md](简历面试逐字稿/项目深挖/RAGAS评测怎么做.md) | 🟡 | 2026-09-11 | 补：RAGAS 五指标全名（faithfulness/answer_relevancy/answer_correctness/context_precision/context_recall，后两项即"上下文准确性"）、延迟口径 TTFT P50/P95/P99、评测数据流程（150 全量/小集 20/冒烟 5/judge 先 10 条/RAGAS 3 次取均值）、A/B 实证数字与目标表；⚠️ **五指标第二次只报出"三个半"**（长鑫 Q4：把 precision+recall 并成"上下文准确度"、漏 answer_correctness）→ 背成肌肉记忆。另：意图评测**没有"三层各自准确率"**，是**只评叶子层 Top-1** 再按 l1/l2 标签做分组切片；**触发澄清的样本不进分母**（长鑫 Q5 答对结论但没讲理由） |
| [文档解析与分块怎么做.md](简历面试逐字稿/项目深挖/文档解析与分块怎么做.md) | 🔴 | — | 补：策略枚举已废弃（切法由文档结构决定、收敛为 ChunkBudget 预算驱动）的 Q6 口径练顺；效果部分无独立消融、端到端是 20 样本冒烟非 150 全量需诚实讲清；fixture 是电商「商家手册」非寿险、示例域与测试域要分清 |
| [SSE流式怎么做.md](简历面试逐字稿/项目深挖/SSE流式怎么做.md) | 🔴 | — | 🆕 **2026-09-16 新登记 · 待背**：15 问端到端（选型 → 事件契约 → 服务端 → 可靠性 → 限流拒绝 → **RAG→Agent 九事件演进** → TTFT 实测 → 双端解析 → 网关）。⚠️ **口径冲突待你拍板**：本篇按「**主负责（流式协议与双端契约是我设计的）**」讲，但本表上方「分工口径固定」只列了意图识别/改写拆分/多路检索/分布式限流四项 → **需决定 SSE 是否进自我介绍的分工列举**。必抛锚点：① **Q4 三处终态不一致**（限流拒绝走流内 `reject` 帧、幂等拦截走流外 JSON、出错直接关连接 → **未统一**，Agent 档已补 `error`+`finish(INTERRUPTED)`，workflow 档未跟上；**主动暴露必讲**）② **Q7 赋值顺序竞态**（`ownerUserId` 必须先于 `finalizer` 落地，否则出现「无主流」；鉴权失败时**连 `cancelled` 都不置**——置了会让 `register` 的 Redis 复核短路，等于绕开鉴权那道门）③ **Q8 `DONE` 优先级高于落库**（落库失败只 `log.error` 不阻断）④ **Q6 发送静默丢弃 ≠ 丢内容**（内容在 `StringBuilder`，落库读它不读发出去的东西）⑤ **Q11 整批推 `awaiting`**（只推卡片点名的会让其余留 `pending` 被判 `interrupted`；整批被拒**不给批次号与起止**——从没执行过，给时间就是撒谎）⑥ **Q12 Agent 不重试**（可能已写过、客户端**无法自证服务端没跑起来**）；RAG 可重试（无副作用）⑦ **Q10 两套协议「有意分立」**（RAG 契约不该背 `confirm`/`tool` 这些它永远没有的状态——**要反向讲成想清楚了的证据**）⑧ **Q15 网关层全仓无配置**（无 Nginx/Dockerfile）→ 按通用知识讲并**显式声明**，判据是帧到达时间戳分布（后端日志永远正常）。数字口径：A/B **20 样本**，A=问题重写+意图树检索 vs B 两组都关，`ttft_p50` 7131→8131（**+1000ms = 两倍于 500ms 显著性阈值，不是噪声**）；质量 `hit@1` +28.5%、`over_retrieval` 100%→0%；⚠️ **归因是事后推的、当时 A/B 打包做没拆单变量（方法失误要主动认，正确做法是拆两组单变量）**；`--ragas-n` 默认 1 **需按 3 次均值重跑**；⚠️ `eval_set_v1.jsonl` 实为 **20 行**（仓库文档写 150 有误，待修正）；5 样本录制 `p50(5342)>mean(4824)` **只能当冒烟不当趋势**。评测侧两个硬伤（主动暴露）：`parse_sse_stream` **零单测**（却是所有评测数字的入口，待补 6 个用例）+ 双请求双检索**不保证一致**（重做会把 chunk 级 id 加进 `finish.sources`） |
| [MQ怎么做.md](简历面试逐字稿/项目深挖/MQ怎么做.md)（🚧 待生成） | 🔴 | — | **已连挂两场（小熊 Q5 卡壳 + 长鑫 Q19 讲反）**：RocketMQ 事务消息两阶段框架（半消息 → 本地事务 → 二次确认）、**回查 = Broker 没收到二次确认时主动回调 `checkLocalTransaction` 去查本地库，不是"本地事务失败后"的动作**（UNKNOWN 继续查、超上限默认回滚）、半消息在 `RMQ_SYS_TRANS_HALF_TOPIC`、commit 是恢复回真实 topic、消费端幂等（状态机/uuid keys）、丢失三环节、三个实际 MQ 场景 |
| [TTL异步链路透传-面试指南.md](简历面试逐字稿/项目深挖/TTL异步链路透传-面试指南.md) | — | — | 📚 参考 · 非逐字稿 |
| [架构技术文档.md](简历面试逐字稿/项目深挖/架构技术文档.md) | — | — | 📚 参考 · 非逐字稿（模块全景） |

### 🧭 项目深挖v2-升级清单 /（Agentic 对照）

> 目录：`简历面试逐字稿/项目深挖v2-升级清单/`。分两类：✍️ **要背**的逐字稿（熟练度追踪）与 📚 **参考**支撑资料（不追踪熟练度）。文件清单见二级索引。

#### ✍️ 要背 · 逐字稿

| 文件 | 熟练度 | 最后复习 | 待补点 |
|------|:--:|:--:|------|
| [MCP怎么做（新）.md](简历面试逐字稿/项目深挖v2-升级清单/MCP怎么做（新）.md) | 🔴 | 2026-09-11 | 补：开场已重构为端到端主线口播 + 单锚点图（原 4 图节并入后删除）；读/写两分支带开句练到自然引 Q#；⚠️ **长鑫 Q9 把最亮的设计讲反**——确认续跑的**工具与入参从 `t_agent_state` 取当初那条原件**（前端**只传 approved=true/false**，接口注释原话"防止篡改"），**不是"从库里拿参数"**（落库落的是 `AWAITING_CONFIRM` 状态的消息）；**"防篡改"提为必抛第一句**。补抛：`AWAITING_CONFIRM` 是唯一非终态 + `hasPendingConfirm` 挡新提问、confirm 块 `pending/approved/denied/expired`、`AgentConfirmDenialMiddleware` 改写英文拒绝文案 |
| [Agent化升级（新老机制对比）.md](简历面试逐字稿/项目深挖v2-升级清单/Agent化升级（新老机制对比）.md) | 🔴 | 2026-09-11 | 补：开场 45s 口播（RAG 流水线 → Agentic 机制主线）背顺；新旧机制逐条对照（编排 / 工具 / 记忆 / 确认…）练到能引「升级清单」；⚠️ **长鑫 Q8/Q10 最大失分点**：面试官连问五道 Agent 题，你却两次说"**没有流程编排、Agent 升级还没做到**""**并不是一个智能体的工作**"——而 v2 已实现（`OrchestrationMode` 两档 + `ConditionalOnAgentEngine` + `ReActAgentProvider`）。**固定口径**：v1 代码编排流水线 / v2 模型编排 ReAct 循环、`ragent.engine.type` 两档可切，**"没做的"只能限定成"没有图 / DAG 式可视化编排引擎"**；引擎名统一说 **AgentScope**（别说成"接入第三方平台"或"只能用 Spring AI"）；⚠️ **Q10 是选型题**（面试官**已知**你用 AgentScope、问"**为什么不用其他智能体框架，比如 Spring AI**"，你答成"这不是智能体、还没做编排"）→ 固定四段：① **定位**（用的就是 AgentScope 2.0.2；v1 代码编排固定流水线 / v2 模型编排 ReAct 循环，两档可切，**没做的只有图 / DAG 可视化编排引擎**）② **看中的运行时能力**（原生 ReAct 循环 / 中间件洋葱 `onReasoning` / 状态存储 SPI→`PgAgentStateStore` / 可中断在环确认）③ **Spring AI 的边界**（强在 Spring 生态模型抽象 ChatClient·Advisor·VectorStore，**不是 agent runtime**；v1 用它够、v2 才需换引擎；⚠️ 不报其版本号 API 名）④ **代价**（自写适配、AgentScope 自带 MCP SDK 0.17.0 被手工压制为 1.1.2 → agent 模块禁用其 mcp 客户端、社区体量小、锁 2.0.2） |
| [记忆系统的三层架构.md](简历面试逐字稿/项目深挖v2-升级清单/记忆系统的三层架构.md) | 🔴 | — | 🆕 **2026-09-15 新登记 · 待背**：三层全景 Q0（短期 / 中期 / 长期）+ Q8 短期 / Q9 中期；核心口径「**载体借 AgentScope、治理自研**」——短期治理 `AgentContextTrimmer` **50% 水位**（可回收量够 20% 才真裁、**等长占位**带原入参、**只换 `tool_result` 不动 `tool_use`**、保护最近 2 个已完成循环 + 未闭合循环、只裁 `evictableTools` 默认 `search_knowledge`）、中期 `AgentContextCompactor` **80% 水位**（末条须为用户消息、留 20% 原文、切点只落用户轮起点、滚动吸收上一代摘要、落 `t_agent_context_compaction`、落库失败只报警不回滚）、长期 `t_agent_memory` 三张表 + 冲突治理；接力顺序 `userMemory → compaction → confirm → skillMasking`；⚠️ **`t_agent_state`（框架短期记忆持久化·不透明 JSON）与 `t_agent_message`（业务消息日志·给前端/审计）是两套** |

#### 📚 参考 · 支撑资料（不追踪熟练度）

- [Ragent升级清单.md](简历面试逐字稿/项目深挖v2-升级清单/Ragent升级清单.md) — v1→v2 升级项总索引（提交时间线 · 升级维度对照 · 代码锚点），v2 系稿件入口文档
- [ragentic架构技术文档.md](简历面试逐字稿/项目深挖v2-升级清单/ragentic架构技术文档.md) — 架构演进视角（Agent 引擎 / 中间件链 / 记忆 / 确认 / 技能），架构技术文档(v1) 姊妹篇

### 🎯 业界方案补充（对标 Claude / 主流方案深挖）

| 文件 | 熟练度 | 最后复习 | 待补点 |
|------|:--:|:--:|------|
| [Claude记忆架构怎么做.md](简历面试逐字稿/项目深挖/Claude记忆架构怎么做.md) | 🟡 | 2026-08-25 | 补：对比 Cursor / Devin 的记忆方案、MemGPT 等开源记忆框架、"记忆即上下文压缩" 与 RAG 的边界 |

### 📚 面试题目 · 八股基础（按主题）

| 文件 | 熟练度 | 最后复习 | 待补点 |
|------|:--:|:--:|------|
| Java集合与并发.md | 🔴 | — | 待生成；**长鑫 Q14 暴露（题面已补正为「JDK 8 和 JDK 17 有什么区别」）**：要背 **JDK 8→17 四条线**——① 语言特性（`var` / switch 表达式 / 文本块 / `instanceof` 模式匹配 / Record / sealed 17 正式）② 标准库（模块化与 17 强封装 `--add-opens` / 集合工厂 / Stream、String 增强 / `HttpClient` 11 正式 / Helpful NPE）③ GC（G1 自 9 默认 / CMS 14 移除 / ZGC、Shenandoah 15 转正）④ 迁移代价（Java EE 模块 11 移除、`javax.*`→`jakarta.*` / 字节码 52→61）；**收尾必说"我们 `ragent` 就跑在 17"**（`pom.xml`）。**虚拟线程是 21 的特性（19/20 预览 → 21 转正 JEP 444），不属于 8→17**；适用 I/O 密集，省的是线程栈 + 阻塞时的平台线程占用（不是"不需要 CPU"），我们现状 `CompletableFuture` + 专用线程池、未用虚拟线程。另补 **Agent 模式对比（ReAct = 单模型反复想和做 / Supervisor = 一个模型当调度器、每轮收 worker 结果再决策；"派完不管"是 Router 不是 Supervisor）**。⚠️ **店小秘 Q6/Q7/Q8 三连暴露，本条优先级提为最高**：① **Q6 快慢指针答非所问**——滑动窗口**不是环**，却拿判环去答，且**漏掉判环题真正考点「环入口」**（只答"有没有环"）→ 背 Floyd 四步：切分场景（窗口用首尾**索引**定位 id 区间）→ 同起点快 2 慢 1 → **有环必相遇**（进环后相对速度恒 1）→ **相遇后一指针回头结点、同速各走一步，再遇即环入口**，收口 O(n)/O(1) 优于哈希表；② **Q7 `FutureTask` 与 `CompletableFuture` 混为一谈**——把 `CompletableFuture.allOf` 的"等最慢的、合并"安到 `FutureTask`（它只封装**单个**任务，无合并能力）→ 必说职责切分「**`FutureTask` = 单任务封装，是 `submit()` 的返回类型；`allOf` = 多任务编排**」（项目里 `load()` 并行拉取用的是后者，`DefaultConversationMemoryService.java:49`）；补 **7 态** `NEW/COMPLETING/NORMAL/EXCEPTIONAL/CANCELLED/INTERRUPTING/INTERRUPTED`（`state` 与 `runner` 均 `UNSAFE.compareAndSwapInt`，**无锁**；`get()` 走 `awaitDone()` = 自旋 + **`LockSupport.park()`**）；③ **Q8 线程池本场最大硬伤**——参数自相矛盾（先说 core=CPU×2，又说 core=CPU、max=CPU×2）、拒绝策略一概而论说"都 CallerRuns"、`SynchronousQueue` 照教科书讲、背压挂错层。**全量口径见 `总结/高频错题.md` 的 10 池 / 6 档表**：没有任何池是 core=CPU×2；`modelStreamExecutor`、`chatEntryExecutor`、`knowledgeChunkExecutor` 是 **AbortPolicy**（非 CallerRuns）；`SynchronousQueue` 容量 0、`offer()` 只在有线程阻塞在 `take()` 时成功 → core 一满几乎立刻扩到 max、然后拒绝，**队列长度恒为 0**，`maxPoolSize` 才是真并发上限、`corePoolSize` 只影响回收 |
| JVM内存与GC.md | 🔴 | — | 待生成 |
| Spring核心.md | 🔴 | — | 待生成 |
| 数据库与Redis.md | 🔴 | — | 待生成；⚠️ **店小秘 Q10/Q11/Q12 三连暴露，本条优先级提为最高**：① **Q10 存储选型两处硬伤**——**编了数据库版本号**（说「公司内部用的是 5.7 / 6」，全仓**无任何数据库版本声明**；"5.7" 唯一一次出现是 `AGENTS.md:27` 的 **Spring Boot 3.5.7**，与数据库无关）+ **把「支持向量」讲成「不支持」**（说"数据库不支持向量所以用 PgVector"，而 **PgVector 就是 PostgreSQL 的扩展**，等于说"因为 PG 不支持所以用 PG"）→ 正确口播「**关系和向量合一：PostgreSQL 装 pgvector 扩展**，**1536 维**，**HNSW + cosine** 索引，检索期设 `hnsw.ef_search = 200` 与 `hnsw.iterative_scan = relaxed_order`；向量后端还可用 `rag.vector.type` 在 `pg` / `milvus` 间切换，默认 pg」（锚点 `schema_pg.sql:5`/`:641`、`application.yaml:105`/`:86`、`PgVectorStoreAdmin.java:39-51`、`PgVectorRetrieverService.java:74-76`）；⚠️ **不要报版本号**（`resources/docker/langfuse/...` 的 `postgres:17` 是 **Langfuse 独立栈**、不可当业务库版本）；⚠️ `AGENTS.md:278` 提到的 `schema_mysql.sql` **文件实际不存在**，别顺势说"MySQL 也支持"；② **Q11 B+ 树与分库分表因果讲反**——说成「表大 → 树高上涨 → 所以分表降树高提索引效率」，而 **B+ 树高度增长极慢**（3~4 层够亿级），**分库分表主因是单机磁盘容量 / 连接数 / 写入吞吐**；并**漏了 B+ 树首要选型理由——叶子双向链表**（范围查询顺链表走不用回根）→ 补：非叶子只存键+指针、数据全在叶子、**叶子双向链表**、InnoDB **按页 16KB** 读写 → 树高每降一层省一次随机 IO；分库分表**代价一起说**（跨库 JOIN 没了、分布式事务 / 全局唯一 ID / 跨分片分页）；③ **Q12 SQL（"所有科目都 >80 的学生"）**——**AVG 与 MIN 口径混**（`AVG>80` 是错的）、**全程没点出 `HAVING`**、**漏「参与所有科目」完整性校验**（只写 `MIN(score)>80` 会把缺考生也选出来）→ 标准答法 `GROUP BY student_id HAVING MIN(score) > 80 AND COUNT(DISTINCT subject) = (SELECT COUNT(DISTINCT subject) FROM score)`；答题顺序：先复述题意 → 说 `GROUP BY`/`HAVING` → 写 `MIN` → 主动补完整性那一行 |

### 🏗️ 面试题目 · 场景设计题

| 文件 | 熟练度 | 最后复习 | 待补点 |
|------|:--:|:--:|------|
| [分块与检索效率.md](面试题目/场景设计题/分块与检索效率.md) | 🔴 | — | 1000 页大文档场景答法：先拆「质量/延迟」→ 四层手段（入库/索引/检索/评测）→ 落到项目。已做到：结构化分块（Block 分发 + 标题分节 + 预算 1024/128/容忍 3 + 边界回溯）/ HNSW / KB 隔离 / 两级缩范围 / 20→RRF→40→重排→10。⚠️ 缺口：无父子块、`metadataFilters` 零使用点、`outlinePath` 未落库、无 page 字段、写库不分批 —— **别讲成「已实现父子块」** |
| 线上排障方法论.md（待生成） | — | — | 本场 Q15 暴露：先止血→分级监控→抓线索→补告警回填 四步骨架 |
| 换域设计题套路.md（待生成） | — | — | 本场 Q17 缴械：建库→切分→召回→评测 + 与自家 RAG 架构差异点；50 万调用骨架：估 QPS→扩容→限流→存储演进（本场 Q10/Q11） |

### ⏳ 挂起待办

- ✅ 已裁决（2026-09-10）：面试叙述统一用**寿险/重疾险域**；技术机制与数字仍以 ragent/ragenteval 代码为唯一锚点（仓库里的企业 IT 模板 / 示例只作核码证据，不进面试叙述）。入口见 `总结/小熊博望/01-一面/复盘.md` 顶部说明。
- ⏳ 长鑫一面（2026-09-11）暴露的三个**结构性动作**（详见 `总结/长鑫/01-一面/复盘.md`）：
  1. **30 秒系统地图开场**——"一条流水线五段 + 两档执行引擎 + 四类执行单元"，先地图后细节。面试官末段反馈原话就是"功能定位与相互关系要介绍得更清楚"，**"有细节、没地图"是重复病**。
  2. **`MQ怎么做.md` 仍未生成（第二次欠账）**——校项目用的就是 RocketMQ，已连挂两场。
  3. **`面试题目/八股基础/` 四个文件全部待生成**——本场又暴露虚拟线程版本错、Agent 模式讲反，八股欠账开始反噬项目分的印象。
- ⏳ **店小秘一面（2026-09-16）暴露的结构性动作**（详见 `总结/店小秘/01-一面/复盘.md`）。本场结论一句话：**面试官反馈「不够专业」——病灶不是表达，是「术语全对、细节全错」**；项目深挖连问五题，其中**会话记忆 / 线程池 / 存储选型三块全崩，而逐字稿里写的都是对的 → 典型「白准备的」**。
  1. **`MQ怎么做.md` 第二次欠账未清，本次虽未考但仍在挂**（延续长鑫第 2 条）。
  2. **30 秒系统地图第三次复发**（长鑫 → 华泰 → 店小秘）——Q1 架构仍讲成"四个层级 + 罗列模块"，面试官听完没有地图。**已到必须动手写开场稿的程度，不再是提醒**。
  3. **分工口径第二次违规**（会话记忆 + 评测又进「深度参与」），且**被面试官误解后没纠正**（Q2）→ 需要一段"被误解时当场纠正并把话题拉回"的固定话术。
  4. **新增待生成：优惠券系统逐字稿**——店小秘 Q13 问了另一个项目，只报名词（防黄牛 / 分库分表 / 设计模式 / 秒杀缓存）没展开。四条亮点需各配一段 40 秒口播 + 一个可追问细节（分表键、防重复领的幂等、缓存与库存一致性）。
  5. **逐字稿数据回源（技术债）**——`会话记忆怎么做.md` 两处数字与 yaml 冲突已定位（摘要字数 200→**400**、`triggerChars 20000`→**裁剪门 5 万 / 压缩门 8 万 / 保留段 2 万**，由 `context-window-chars: 100000` 派生），**待改稿**，否则下次照旧讲错。

> 📝 复盘沉淀的文件清单（高频错题 · 各场复盘 + 原文）已下沉到二级索引 [`总结/README.md`](总结/README.md)；状态性待办保留在本表。

> 🎭 提示词工具清单（8 个：角色扮演 / 逐字稿生成 / 整理复盘 / 资料库维护）已下沉到二级索引 [`提示词/README.md`](提示词/README.md)，本表不再重复。

---

## 🧩 新建逐字稿统一模板

```markdown
> 简历产出：（一句话，来自简历原文）
> 覆盖模块：（模块名）
> 熟练度：🔴
> 最后复习：（日期）
```

---

## 🔄 工作流

1. **练**：用「提示词/面试提示词.md」开一场面试
2. **存**：新模块 → 套模板新建逐字稿；已有模块 → 在原稿追加问答
3. **登**：新增 / 移动文档后 → 跑「提示词/索引登记提示词.md」登记两级索引（① 回本表登记或更新状态行，② 到所在区块 README 补一行链接 + 定位）
4. **跟**：回本表更新熟练度、日期、待补点
5. **复盘**：每场面试后用「面试整理提示词」沉淀实录，再跑「复盘答案提示词」逐题复盘（当时怎么答 + 代码优先的标准答案）、追加高频错题并回填本表
